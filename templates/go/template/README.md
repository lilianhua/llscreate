# <%= projectName %>

Created with `llscreate` · Go 1.23.

Author: <%= answers.authorName %>

## Prerequisites

- Go 1.23+

## Getting started

```bash
go run main.go
```

## Scripts

- `go run main.go` — 直接运行
- `go build` — 编译为可执行文件
- `go test ./...` — 跑测试（暂未配置）
