# <%= projectName %>

Created with `llscreate` · Rust + Cargo (Edition 2021).

Author: <%= answers.authorName %>

## Prerequisites

- Rust stable (rustup recommended)

## Getting started

```bash
cargo run
```

## Scripts

- `cargo run` — 编译并运行
- `cargo build` — 编译（debug）
- `cargo build --release` — 编译（release）
- `cargo test` — 跑测试（暂未配置）
