# <%= projectName %>

Created with `llscreate` · TypeScript + tsx (directly runnable).

Author: <%= answers.authorName %>

## Getting started

```bash
npm install
npm start
```

## Scripts

- `npm start` — run `src/index.ts` via tsx
- `npm run dev` — run with watch mode
- `npm run build` — type-check and emit to `dist/`
